```shell
pip install -r requirements.txt
# FLASH_ATTENTION_SKIP_CUDA_BUILD=TRUE pip install flash-attn --no-build-isolation
# pip install yt-dlp # in case you want it to download yt video
python app.py
or
python query_cli.py <video_path>
```
